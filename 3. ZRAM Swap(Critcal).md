3. ZRAM Swap (CRITICAL)

ZRAM is REQUIRED to avoid OOM crashes.

📄 Script: setup/zram_setup.sh

Manual steps:

sudo pacman -S zram-generator


Config:

[zram0]
zram-size = ram
compression-algorithm = zstd


Enable:

sudo systemctl daemon-reexec
sudo systemctl start systemd-zram-setup@zram0
swapon --show


Why this works:

Compressed RAM swap (≈2–3× effective memory)

Faster than disk swap

Prevents kernel OOM killer
